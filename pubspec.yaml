name: shredded_squad_2
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
    flutter:
      sdk: flutter
    image_picker: ^1.0.7
    shared_preferences: ^2.2.3
    sqflite: ^2.3.2
    path: ^1.9.0
    http: ^1.2.1
    firebase_core: ^3.1.0
    firebase_auth: ^5.1.0
    cloud_firestore: ^5.1.0
    cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design:  true